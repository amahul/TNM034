function skinImage = skinDetection(inputImg)
% Functionthe facemask in color

binaryImg = face_mask(inputImg);

%Multiplying the binary image with the original image, we get a facemask
%with skin color
test = binaryImg.*im2double(inputImg);

skinImage = inputImg;

imshow(test);

end